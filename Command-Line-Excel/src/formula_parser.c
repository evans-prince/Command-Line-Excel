#include "../include/formula_parser.h"
#include "../include/utils.h"

#include<string.h>

int eval_formula(char * expr){
    
    return -123;
}

char **parse_formula(const char *formula , int *dep_count){ // char** ans is just a pointer of array of strings
    // this function needs to find and return all dependencies from formula
    // that is all cells name from formula and update its count in dep_count
    return NULL;
}
